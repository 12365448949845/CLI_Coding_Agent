# mewcode

MewCode is a terminal conversation client that supports Anthropic and OpenAI-compatible providers.

## Development

```text
python -m mewcode
```

Runtime configuration is read from `.mewcode/config.yaml`. See `.mewcode/config.yaml.example` for the expected shape.
